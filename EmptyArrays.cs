using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using Osprey.Nodes;

namespace Osprey
{
	internal static class EmptyArrays
	{
		// In alphabetic order

		internal static readonly CatchClause[] CatchClauses = new CatchClause[0];

		internal static readonly ConstructorParam[] CtorParameters = new ConstructorParam[0];

		internal static readonly Expression[] Expressions = new Expression[0];

		internal static readonly HashMember[] HashMembers = new HashMember[0];

		internal static readonly MemberInitializer[] MemberInitializers = new MemberInitializer[0];

		internal static readonly Parameter[] Parameters = new Parameter[0];

		internal static readonly Statement[] Statements = new Statement[0];
	}
}